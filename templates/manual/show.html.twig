{% extends 'base.html.twig' %}

{% block title %}Manual{% endblock %}

{% block body %}
    <h1>Manual</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manual.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ manual.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ manual.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('manual_index') }}">back to list</a>

    <a href="{{ path('manual_edit', {'id': manual.id}) }}">edit</a>

    {{ include('manual/_delete_form.html.twig') }}
{% endblock %}
