{% extends 'layout.html.twig' %}

{% block main %}
    {{ form_start(form) }}
    {% for activity in form.activities %}
        {{ form_row(activity, {'label': activity.vars.data.name}) }}
    {% endfor %}
    {{ form_end(form) }}
{% endblock %}