{% form_theme form 'bootstrap_4_layout.html.twig' %}
<section class="container-fluid my-5 w-25">
    <h3>Charger les fichiers</h3>
    <br>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-around">
        <button class="btn btn-lg btn-primary">{{ button_label|default('Valider') }}</button>
    </div>
    {{ form_end(form) }}
</section>
