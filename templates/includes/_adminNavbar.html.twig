<nav class="nav navbar-expand-lg">
    <a href="{{ path('home') }}">
        <img class="logo-navbar" src="{{ asset('build/images/Logo-Cotoit.png') }}" alt="Logo-Cotoit"></a>

    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="navbar-link nav-link" href="https://www.cotoit.fr" target = "_blank">En savoir plus</a>
            </li>
            <li class="nav-item">
                <a class="navbar-link nav-link" href="{{ path('home') }}">Accueil</a>
            </li>
            <li>
                {% if is_granted("ROLE_ADMIN") %}
                    <a class="navbar-link nav-link" href="{{ path('admin') }}">Administration</a>
                {%endif%}
            </li>
            <li class="nav-item">
                {% if app.user %}
                    <a class="navbar-link nav-link" href="{{ path('app_logout') }}"> Se déconnecter</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>
