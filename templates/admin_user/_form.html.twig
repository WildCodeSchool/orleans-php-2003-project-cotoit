{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="my-4">
    <button class="btn btn-primary mr-4">Enregistrer</button>
    <a class="btn btn-primary" href="{{ path('admin_user_index') }}">Retour</a>
</div>
{{ form_end(form) }}
